import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF3F80DE);   // Azure Blue
  static const Color secondaryBlue = Color(0xFF4866E8); // Indigo Blue
  static const Color white = Color(0xFFFFFFFF);         // White
  static const Color saffron = Color(0xFFF6AE2D);       // Saffron Yellow
  static const Color tomatoRed = Color(0xFFFF6347);     // Tomato Red
  static const Color deepBlue = Color(0xFF2337AA);      // Cobalt / Royal Blue
  
}
